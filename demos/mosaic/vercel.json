{
  "buildCommand": "pnpm --filter @arcana-ui/tokens build && pnpm --filter @arcana-ui/demo-mosaic build",
  "outputDirectory": "demos/mosaic/dist",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
