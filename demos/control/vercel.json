{
  "buildCommand": "pnpm --filter @arcana-ui/tokens build && pnpm --filter @arcana-ui/demo-control build",
  "outputDirectory": "demos/control/dist",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
