/**
 * The version of `@arcana-ui/core` currently in use.
 *
 * This constant is the single source of truth for the runtime-exposed package
 * version. It is updated automatically during the release process (see
 * `RELEASING.md`) and re-exported from the package entry point so that
 * consumers, the CLI (`arcana-ui info --version`), and AI agents can read it
 * without touching the filesystem.
 */
export const VERSION = '0.1.0';
