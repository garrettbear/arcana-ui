{
  "name": "@arcana-ui/core",
  "version": "0.1.0",
  "description": "AI-first React design system with token-driven theming. Warm, clean, accessible. Built for machines, beautiful for humans.",
  "install": "npm i @arcana-ui/core @arcana-ui/tokens",
  "quickStart": [
    "import '@arcana-ui/tokens/dist/arcana.css'",
    "import { Button, Card, Input } from '@arcana-ui/core'",
    "",
    "// Theme switching — no JS runtime, just CSS",
    "document.documentElement.setAttribute('data-theme', 'dark')"
  ],
  "themeSetup": "Set data-theme='light' or data-theme='dark' on <html> element",
  "tokens": {
    "categories": ["primitive", "semantic", "component"],
    "naming": "Three-tier architecture: primitives (raw values) → semantic (contextual) → component (scoped)",
    "themeSwitch": "Set data-theme attribute on root element: 'light' | 'dark' | 'terminal' | 'retro98' | 'glass' | 'brutalist'",
    "customization": "Override any CSS custom property in your CSS to customize. All tokens are prefixed with -- (e.g., --color-action-primary)",
    "keyTokens": {
      "--color-action-primary": "Primary brand color for actions (button, link, highlight)",
      "--color-bg-page": "Main background surface for page content",
      "--color-fg-primary": "Primary text color (body text, labels)",
      "--radius-md": "Medium border radius (commonly used in components)",
      "--shadow-md": "Medium drop shadow for elevation",
      "--spacing-md": "Medium spacing unit (used for padding, margin, gaps)",
      "--font-size-base": "Base text size for body copy",
      "--duration-normal": "Default animation duration for transitions"
    }
  },
  "components": [
    {
      "name": "Button",
      "import": "import { Button } from '@arcana-ui/core'",
      "description": "Primary action trigger. Renders as <button>. Use for form submissions, CTAs, confirmations, and navigation actions.",
      "when": "User needs to trigger an action or navigate",
      "props": {
        "variant": {
          "type": "enum",
          "values": ["primary", "secondary", "ghost", "destructive", "outline"],
          "default": "primary",
          "description": "Visual style of the button"
        },
        "size": {
          "type": "enum",
          "values": ["sm", "md", "lg"],
          "default": "md",
          "description": "Button size (32px / 40px / 48px height)"
        },
        "loading": {
          "type": "boolean",
          "default": false,
          "description": "Shows spinner, disables interaction"
        },
        "disabled": { "type": "boolean", "default": false },
        "icon": { "type": "ReactNode", "description": "Leading icon element" },
        "iconRight": { "type": "ReactNode", "description": "Trailing icon element" },
        "fullWidth": {
          "type": "boolean",
          "default": false,
          "description": "Stretches button to fill container"
        }
      },
      "examples": [
        "<Button>Save changes</Button>",
        "<Button variant=\"secondary\" size=\"sm\">Cancel</Button>",
        "<Button variant=\"destructive\" loading>Deleting...</Button>",
        "<Button variant=\"ghost\" icon={<PlusIcon />}>Add item</Button>",
        "<Button variant=\"outline\">Learn more</Button>",
        "<Button fullWidth>Sign in</Button>"
      ],
      "accessibility": "Renders as <button>. Include text or aria-label. Loading state sets aria-busy."
    },
    {
      "name": "Input",
      "import": "import { Input } from '@arcana-ui/core'",
      "description": "Text input field with label, error, and prefix/suffix slot support. Forwards ref.",
      "when": "User needs to enter text: name, email, password, search, etc.",
      "props": {
        "label": { "type": "string", "description": "Label above the input" },
        "error": {
          "type": "string | boolean",
          "description": "Error message or true to show error styling"
        },
        "helperText": { "type": "string", "description": "Helper text below input" },
        "prefix": { "type": "ReactNode", "description": "Icon or text prepended inside input" },
        "suffix": { "type": "ReactNode", "description": "Icon or text appended inside input" },
        "fullWidth": { "type": "boolean", "default": false },
        "type": {
          "type": "string",
          "description": "HTML input type: text, email, password, search, etc."
        }
      },
      "examples": [
        "<Input label=\"Email\" type=\"email\" placeholder=\"you@example.com\" />",
        "<Input label=\"Search\" prefix={<SearchIcon />} placeholder=\"Search...\" />",
        "<Input label=\"Username\" error=\"Username is already taken\" />",
        "<Input label=\"Name\" helperText=\"Full legal name\" fullWidth />"
      ],
      "accessibility": "Auto-generates id. Links label with htmlFor. Sets aria-invalid and aria-describedby."
    },
    {
      "name": "Textarea",
      "import": "import { Textarea } from '@arcana-ui/core'",
      "description": "Multi-line text input with optional auto-resize and character counter.",
      "when": "User needs to enter longer text: bio, description, message, notes",
      "props": {
        "label": { "type": "string" },
        "error": { "type": "string | boolean" },
        "helperText": { "type": "string" },
        "autoResize": {
          "type": "boolean",
          "default": false,
          "description": "Grows with content automatically"
        },
        "showCount": {
          "type": "boolean",
          "default": false,
          "description": "Shows character count"
        },
        "maxLength": {
          "type": "number",
          "description": "Max characters. Shows X/max counter when showCount=true"
        }
      },
      "examples": [
        "<Textarea label=\"Bio\" placeholder=\"Tell us about yourself\" />",
        "<Textarea label=\"Message\" autoResize showCount maxLength={500} />"
      ]
    },
    {
      "name": "Select",
      "import": "import { Select } from '@arcana-ui/core'",
      "description": "Native-styled dropdown select with label and error support.",
      "when": "User needs to choose from a list of options",
      "props": {
        "label": { "type": "string" },
        "error": { "type": "string | boolean" },
        "helperText": { "type": "string" },
        "placeholder": {
          "type": "string",
          "description": "Disabled first option shown before selection"
        },
        "options": {
          "type": "SelectOption[]",
          "description": "Array of {value, label, disabled?} objects"
        },
        "fullWidth": { "type": "boolean", "default": false }
      },
      "examples": [
        "<Select label=\"Country\" placeholder=\"Select a country\" options={[{value:'us', label:'United States'}, {value:'uk', label:'UK'}]} />",
        "<Select label=\"Framework\" value={val} onChange={e => setVal(e.target.value)} options={options} />"
      ]
    },
    {
      "name": "Checkbox",
      "import": "import { Checkbox } from '@arcana-ui/core'",
      "description": "Single checkbox with label, description, and indeterminate state.",
      "when": "User needs to toggle a boolean option or select multiple items from a list",
      "props": {
        "label": { "type": "string" },
        "description": { "type": "string", "description": "Helper text below label" },
        "checked": { "type": "boolean" },
        "indeterminate": {
          "type": "boolean",
          "default": false,
          "description": "Shows dash (partial selection)"
        },
        "onChange": { "type": "ChangeEventHandler<HTMLInputElement>" },
        "disabled": { "type": "boolean", "default": false }
      },
      "examples": [
        "<Checkbox label=\"Accept terms\" checked={agreed} onChange={e => setAgreed(e.target.checked)} />",
        "<Checkbox label=\"Select all\" indeterminate={someSelected} checked={allSelected} onChange={handleSelectAll} />"
      ]
    },
    {
      "name": "Radio / RadioGroup",
      "import": "import { Radio, RadioGroup } from '@arcana-ui/core'",
      "description": "Single radio or grouped radio buttons with descriptions. RadioGroup is the preferred pattern.",
      "when": "User must choose exactly one option from a small set",
      "props": {
        "RadioGroup.name": {
          "type": "string",
          "description": "HTML name attribute for the radio group"
        },
        "RadioGroup.label": { "type": "string", "description": "Group legend" },
        "RadioGroup.value": { "type": "string", "description": "Currently selected value" },
        "RadioGroup.onChange": { "type": "(value: string) => void" },
        "RadioGroup.options": {
          "type": "RadioOption[]",
          "description": "Array of {value, label, description?, disabled?}"
        }
      },
      "examples": [
        "<RadioGroup name=\"plan\" label=\"Select plan\" value={plan} onChange={setPlan} options={[{value:'free', label:'Free'}, {value:'pro', label:'Pro', description:'$9/mo'}]} />"
      ]
    },
    {
      "name": "Toggle",
      "import": "import { Toggle } from '@arcana-ui/core'",
      "description": "On/off switch with label. Uses role=switch for accessibility.",
      "when": "User toggles a feature on or off (settings, preferences)",
      "props": {
        "label": { "type": "string" },
        "checked": { "type": "boolean" },
        "onChange": {
          "type": "(checked: boolean) => void",
          "description": "Receives boolean (not event)"
        },
        "size": { "type": "enum", "values": ["sm", "md", "lg"], "default": "md" },
        "disabled": { "type": "boolean", "default": false }
      },
      "examples": [
        "<Toggle label=\"Email notifications\" checked={enabled} onChange={setEnabled} />",
        "<Toggle label=\"Dark mode\" checked={isDark} onChange={setIsDark} size=\"lg\" />"
      ]
    },
    {
      "name": "Badge",
      "import": "import { Badge } from '@arcana-ui/core'",
      "description": "Small status label for counts, states, or categories.",
      "when": "Display status, count, or category label inline",
      "props": {
        "variant": {
          "type": "enum",
          "values": ["default", "success", "warning", "error", "info", "secondary"],
          "default": "default"
        },
        "dot": {
          "type": "boolean",
          "default": false,
          "description": "Shows colored dot before text"
        }
      },
      "examples": [
        "<Badge>Beta</Badge>",
        "<Badge variant=\"success\">Active</Badge>",
        "<Badge variant=\"error\" dot>Offline</Badge>",
        "<Badge variant=\"info\">v0.1.0</Badge>"
      ]
    },
    {
      "name": "Avatar / AvatarGroup",
      "import": "import { Avatar, AvatarGroup } from '@arcana-ui/core'",
      "description": "User avatar with image, initials fallback, and group stacking.",
      "when": "Display user identity: profile picture, initials, or group of users",
      "props": {
        "Avatar.src": {
          "type": "string",
          "description": "Image URL. Falls back to initials on error."
        },
        "Avatar.name": {
          "type": "string",
          "description": "Full name — used for initials and accessible label"
        },
        "Avatar.alt": { "type": "string" },
        "Avatar.size": {
          "type": "enum",
          "values": ["xs", "sm", "md", "lg", "xl"],
          "default": "md"
        },
        "AvatarGroup.max": {
          "type": "number",
          "description": "Max avatars to show before +N overflow indicator"
        }
      },
      "examples": [
        "<Avatar name=\"Alice Zhao\" size=\"md\" />",
        "<Avatar src=\"/avatar.jpg\" name=\"Bob\" size=\"lg\" />",
        "<AvatarGroup max={3}><Avatar name=\"Alice\" /><Avatar name=\"Bob\" /><Avatar name=\"Carlos\" /><Avatar name=\"Diana\" /></AvatarGroup>"
      ]
    },
    {
      "name": "Card / CardHeader / CardBody / CardFooter",
      "import": "import { Card, CardHeader, CardBody, CardFooter } from '@arcana-ui/core'",
      "description": "Flexible container for grouping related content. Composed of optional Header, Body, and Footer slots.",
      "when": "Group related content: dashboard widgets, list items, profile sections, forms",
      "props": {
        "Card.variant": {
          "type": "enum",
          "values": ["default", "outlined", "elevated"],
          "default": "default"
        },
        "Card.padding": { "type": "enum", "values": ["none", "sm", "md", "lg"], "default": "md" },
        "Card.interactive": {
          "type": "boolean",
          "default": false,
          "description": "Adds hover lift effect"
        },
        "CardHeader.title": { "type": "string" },
        "CardHeader.description": { "type": "string" },
        "CardHeader.action": { "type": "ReactNode", "description": "Slotted action in top-right" },
        "CardFooter.align": {
          "type": "enum",
          "values": ["left", "center", "right", "space-between"],
          "default": "right"
        }
      },
      "examples": [
        "<Card><CardHeader title=\"Profile\" description=\"Manage your info\" /><CardBody>...</CardBody><CardFooter><Button size=\"sm\">Save</Button></CardFooter></Card>",
        "<Card variant=\"elevated\" interactive onClick={...}><CardBody>Clickable card</CardBody></Card>"
      ]
    },
    {
      "name": "Modal",
      "import": "import { Modal } from '@arcana-ui/core'",
      "description": "Dialog overlay with focus trap, escape-to-close, and portal rendering.",
      "when": "Confirm destructive actions, display forms, show detail overlays",
      "props": {
        "open": { "type": "boolean", "description": "Controls visibility" },
        "onClose": { "type": "() => void" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "size": { "type": "enum", "values": ["sm", "md", "lg", "xl", "full"], "default": "md" },
        "footer": { "type": "ReactNode", "description": "Action buttons rendered in footer" },
        "closeOnOverlayClick": { "type": "boolean", "default": true },
        "closeOnEsc": { "type": "boolean", "default": true }
      },
      "examples": [
        "<Modal open={open} onClose={() => setOpen(false)} title=\"Delete item\" description=\"This cannot be undone.\" footer={<><Button variant=\"secondary\" onClick={...}>Cancel</Button><Button variant=\"danger\" onClick={...}>Delete</Button></>}><p>Confirmation content here.</p></Modal>"
      ],
      "accessibility": "role=dialog, aria-modal, focus trap, Escape closes, focus restored on close."
    },
    {
      "name": "Alert",
      "import": "import { Alert } from '@arcana-ui/core'",
      "description": "Inline status message for info, success, warning, or error states.",
      "when": "Show form validation results, operation outcomes, or important notices inline",
      "props": {
        "variant": {
          "type": "enum",
          "values": ["info", "success", "warning", "error"],
          "default": "info"
        },
        "title": { "type": "string" },
        "onClose": { "type": "() => void", "description": "Shows X button if provided" }
      },
      "examples": [
        "<Alert variant=\"success\" title=\"Saved!\">Your changes have been saved.</Alert>",
        "<Alert variant=\"error\" title=\"Error\" onClose={dismiss}>Failed to save. Try again.</Alert>",
        "<Alert variant=\"warning\">This action cannot be undone.</Alert>"
      ]
    },
    {
      "name": "ToastProvider / useToast",
      "import": "import { ToastProvider, useToast } from '@arcana-ui/core'",
      "description": "Toast notification system. Wrap app in ToastProvider, then call toast() from useToast hook.",
      "when": "Show transient success/error/info messages after async operations",
      "setup": "Wrap your app root: <ToastProvider><App /></ToastProvider>",
      "props": {
        "toast.title": { "type": "string" },
        "toast.description": { "type": "string" },
        "toast.variant": { "type": "enum", "values": ["default", "success", "warning", "error"] },
        "toast.duration": {
          "type": "number",
          "default": 5000,
          "description": "Auto-dismiss in ms"
        },
        "toast.action": {
          "type": "{ label: string, onClick: () => void }",
          "description": "Inline action button"
        }
      },
      "examples": [
        "const { toast } = useToast()",
        "toast({ title: 'Saved!', variant: 'success' })",
        "toast({ title: 'Error', description: 'Please try again.', variant: 'error' })",
        "toast({ title: 'Deleted', action: { label: 'Undo', onClick: handleUndo } })"
      ]
    },
    {
      "name": "Tabs / TabList / Tab / TabPanels / TabPanel",
      "import": "import { Tabs, TabList, Tab, TabPanels, TabPanel } from '@arcana-ui/core'",
      "description": "Accessible tab navigation. Supports controlled/uncontrolled, line and pills variants.",
      "when": "Organize related content into named sections: settings, details, views",
      "props": {
        "Tabs.value": { "type": "string", "description": "Controlled active tab value" },
        "Tabs.onChange": { "type": "(value: string) => void" },
        "Tabs.defaultValue": {
          "type": "string",
          "description": "Initial active tab (uncontrolled)"
        },
        "Tabs.variant": { "type": "enum", "values": ["line", "pills"], "default": "line" },
        "Tab.value": {
          "type": "string",
          "description": "Tab identifier, must match TabPanel value"
        },
        "Tab.disabled": { "type": "boolean", "default": false }
      },
      "examples": [
        "<Tabs defaultValue=\"overview\"><TabList><Tab value=\"overview\">Overview</Tab><Tab value=\"settings\">Settings</Tab></TabList><TabPanels><TabPanel value=\"overview\">...</TabPanel><TabPanel value=\"settings\">...</TabPanel></TabPanels></Tabs>",
        "<Tabs variant=\"pills\" defaultValue=\"all\"><TabList><Tab value=\"all\">All</Tab><Tab value=\"active\">Active</Tab></TabList></Tabs>"
      ]
    },
    {
      "name": "Accordion / AccordionItem / AccordionTrigger / AccordionContent",
      "import": "import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@arcana-ui/core'",
      "description": "Collapsible content panels. Supports single or multiple expand modes.",
      "when": "FAQs, settings sections, collapsible content groups",
      "props": {
        "Accordion.type": { "type": "enum", "values": ["single", "multiple"], "default": "single" },
        "Accordion.defaultValue": {
          "type": "string | string[]",
          "description": "Initially open item(s)"
        },
        "AccordionItem.value": { "type": "string" },
        "AccordionItem.disabled": { "type": "boolean", "default": false }
      },
      "examples": [
        "<Accordion type=\"single\" defaultValue=\"q1\"><AccordionItem value=\"q1\"><AccordionTrigger>What is Arcana?</AccordionTrigger><AccordionContent>An AI-first design system.</AccordionContent></AccordionItem></Accordion>",
        "<Accordion type=\"multiple\"><AccordionItem value=\"a\"><AccordionTrigger>Section A</AccordionTrigger><AccordionContent>...</AccordionContent></AccordionItem></Accordion>"
      ]
    },
    {
      "name": "Stack",
      "import": "import { Stack } from '@arcana-ui/core'",
      "description": "Vertical flex container with consistent gap spacing using Arcana spacing scale.",
      "when": "Stack elements vertically with consistent spacing",
      "props": {
        "gap": {
          "type": "number",
          "default": 4,
          "description": "Spacing token number (1=4px, 2=8px, 4=16px, 6=24px, 8=32px)"
        },
        "align": { "type": "string", "description": "CSS alignItems value" },
        "justify": { "type": "string", "description": "CSS justifyContent value" }
      },
      "examples": [
        "<Stack gap={4}><Input label=\"Name\" /><Input label=\"Email\" /><Button>Submit</Button></Stack>",
        "<Stack gap={2} align=\"center\">...</Stack>"
      ]
    },
    {
      "name": "HStack",
      "import": "import { HStack } from '@arcana-ui/core'",
      "description": "Horizontal flex container with consistent gap spacing.",
      "when": "Arrange elements side by side: button groups, form rows, icon+text combos",
      "props": {
        "gap": { "type": "number", "default": 4 },
        "align": { "type": "string", "default": "center" },
        "justify": {
          "type": "string",
          "description": "CSS justifyContent. Use 'flex-end' for right-aligned buttons"
        }
      },
      "examples": [
        "<HStack gap={2} justify=\"flex-end\"><Button variant=\"secondary\">Cancel</Button><Button>Save</Button></HStack>",
        "<HStack gap={3} align=\"flex-start\">...</HStack>"
      ]
    },
    {
      "name": "Grid",
      "import": "import { Grid } from '@arcana-ui/core'",
      "description": "CSS Grid wrapper with responsive column support.",
      "when": "Multi-column layouts: card grids, form field rows, dashboard widgets",
      "props": {
        "columns": {
          "type": "number | string",
          "default": 2,
          "description": "Number of equal columns or CSS grid-template-columns string"
        },
        "gap": { "type": "number", "description": "Spacing token for both row and column gap" }
      },
      "examples": [
        "<Grid columns={3} gap={6}><Card /><Card /><Card /></Grid>",
        "<Grid columns={2} gap={4}><Input label=\"First name\" /><Input label=\"Last name\" /></Grid>"
      ]
    },
    {
      "name": "Container",
      "import": "import { Container } from '@arcana-ui/core'",
      "description": "Centered max-width container with responsive padding.",
      "when": "Constrain page content width and center it",
      "props": {
        "size": {
          "type": "enum",
          "values": ["sm", "md", "lg", "xl", "2xl", "full"],
          "default": "lg",
          "description": "Max-width preset: sm=640px, md=768px, lg=1024px, xl=1280px"
        }
      },
      "examples": [
        "<Container size=\"xl\"><main>Page content</main></Container>",
        "<Container><section>Centered content</section></Container>"
      ]
    },
    {
      "name": "Navbar / NavbarBrand / NavbarContent / NavbarActions",
      "import": "import { Navbar, NavbarBrand, NavbarContent, NavbarActions } from '@arcana-ui/core'",
      "description": "Responsive navigation header with brand, nav links, and action slots.",
      "when": "Top-level site navigation",
      "props": {
        "Navbar.sticky": {
          "type": "boolean",
          "default": false,
          "description": "position: sticky top: 0"
        },
        "Navbar.border": {
          "type": "boolean",
          "default": false,
          "description": "Adds bottom border"
        }
      },
      "examples": [
        "<Navbar sticky border><NavbarBrand><Logo /></NavbarBrand><NavbarContent><a href=\"/\">Home</a><a href=\"/docs\">Docs</a></NavbarContent><NavbarActions><Button size=\"sm\">Sign in</Button></NavbarActions></Navbar>"
      ]
    },
    {
      "name": "EmptyState",
      "import": "import { EmptyState } from '@arcana-ui/core'",
      "description": "Centered empty state with icon, title, description, and optional action.",
      "when": "No data to display: empty tables, no search results, first-time empty views",
      "props": {
        "icon": { "type": "ReactNode" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "action": { "type": "ReactNode", "description": "Call-to-action button or link" },
        "size": { "type": "enum", "values": ["sm", "md"], "default": "md" }
      },
      "examples": [
        "<EmptyState icon={<FolderIcon />} title=\"No files yet\" description=\"Upload your first file to get started.\" action={<Button>Upload file</Button>} />",
        "<EmptyState size=\"sm\" title=\"No results\" description=\"Try a different search term.\" />"
      ]
    },
    {
      "name": "Form / FormField / FormLabel / FormHelperText / FormErrorMessage",
      "import": "import { Form, FormField, FormLabel, FormHelperText, FormErrorMessage } from '@arcana-ui/core'",
      "description": "Form wrapper with field-level validation state management via context.",
      "when": "Building forms with labels, helper text, and validation messages",
      "props": {
        "FormField.isRequired": {
          "type": "boolean",
          "default": false,
          "description": "Shows asterisk on label"
        },
        "FormField.isInvalid": {
          "type": "boolean",
          "default": false,
          "description": "Shows error message, hides helper text"
        }
      },
      "examples": [
        "<Form onSubmit={handleSubmit}><FormField isRequired><FormLabel>Email</FormLabel><Input type=\"email\" /><FormHelperText>We'll never share your email.</FormHelperText></FormField><FormField isInvalid={!!errors.name}><FormLabel>Name</FormLabel><Input /><FormErrorMessage>{errors.name}</FormErrorMessage></FormField></Form>"
      ]
    },
    {
      "name": "Table / TableHeader / TableBody / TableRow / TableHead / TableCell",
      "import": "import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from '@arcana-ui/core'",
      "description": "Styled data table with striped rows, hover states, and sortable column headers.",
      "when": "Display tabular data: user lists, transaction history, analytics, settings",
      "props": {
        "Table.striped": {
          "type": "boolean",
          "default": false,
          "description": "Alternating row background"
        },
        "Table.hoverable": {
          "type": "boolean",
          "default": false,
          "description": "Row background on hover"
        },
        "TableHead.sortable": { "type": "boolean", "default": false },
        "TableHead.sortDirection": {
          "type": "enum",
          "values": ["asc", "desc", null],
          "description": "Current sort direction"
        },
        "TableHead.onSort": { "type": "() => void" }
      },
      "examples": [
        "<Table striped hoverable><TableHeader><TableRow><TableHead sortable sortDirection=\"asc\" onSort={sortByName}>Name</TableHead><TableHead>Role</TableHead></TableRow></TableHeader><TableBody>{users.map(u => <TableRow key={u.id}><TableCell>{u.name}</TableCell><TableCell>{u.role}</TableCell></TableRow>)}</TableBody></Table>"
      ]
    },
    {
      "name": "Sidebar",
      "import": "import { Sidebar, SidebarHeader, SidebarContent, SidebarFooter, SidebarSection, SidebarItem } from '@arcana-ui/core'",
      "description": "Collapsible navigation sidebar with icon-only mode, sections, and badge counts.",
      "when": "Build dashboard or admin layouts with persistent navigation",
      "props": {
        "collapsed": {
          "type": "boolean",
          "default": false,
          "description": "Whether sidebar is in icon-only mode"
        },
        "position": { "type": "enum", "values": ["left", "right"], "default": "left" },
        "SidebarItem.icon": { "type": "ReactNode", "description": "Icon element" },
        "SidebarItem.active": { "type": "boolean", "default": false },
        "SidebarItem.badge": {
          "type": "string | number",
          "description": "Badge count on the right"
        },
        "SidebarItem.href": {
          "type": "string",
          "description": "Renders as anchor instead of button"
        }
      },
      "examples": [
        "<Sidebar><SidebarSection label=\"Main\"><SidebarItem icon={<HomeIcon />} active>Dashboard</SidebarItem><SidebarItem icon={<InboxIcon />} badge={5}>Inbox</SidebarItem></SidebarSection></Sidebar>"
      ]
    },
    {
      "name": "Breadcrumb",
      "import": "import { Breadcrumb, BreadcrumbItem } from '@arcana-ui/core'",
      "description": "Hierarchical navigation breadcrumb with truncation support.",
      "when": "Show page location within a site hierarchy",
      "props": {
        "separator": {
          "type": "ReactNode",
          "default": "chevron icon",
          "description": "Custom separator"
        },
        "maxItems": { "type": "number", "description": "Truncate middle items with ellipsis" },
        "BreadcrumbItem.href": { "type": "string", "description": "Link URL" },
        "BreadcrumbItem.current": {
          "type": "boolean",
          "default": false,
          "description": "Current page (no link)"
        }
      },
      "examples": [
        "<Breadcrumb><BreadcrumbItem href=\"/\">Home</BreadcrumbItem><BreadcrumbItem href=\"/products\">Products</BreadcrumbItem><BreadcrumbItem current>Widget</BreadcrumbItem></Breadcrumb>"
      ]
    },
    {
      "name": "Pagination",
      "import": "import { Pagination } from '@arcana-ui/core'",
      "description": "Page navigation with prev/next, page numbers, ellipsis, and compact variant.",
      "when": "Navigate through paginated data or content",
      "props": {
        "page": { "type": "number", "description": "Current page (1-indexed)" },
        "totalPages": { "type": "number", "description": "Total number of pages" },
        "onPageChange": { "type": "(page: number) => void" },
        "siblingCount": { "type": "number", "default": 1 },
        "showEdges": {
          "type": "boolean",
          "default": true,
          "description": "Show first/last page buttons"
        },
        "variant": {
          "type": "enum",
          "values": ["default", "compact"],
          "default": "default",
          "description": "Compact shows 'Page X of Y'"
        }
      },
      "examples": [
        "<Pagination page={currentPage} totalPages={20} onPageChange={setPage} />",
        "<Pagination page={page} totalPages={total} onPageChange={setPage} variant=\"compact\" />"
      ]
    },
    {
      "name": "Footer",
      "import": "import { Footer, FooterSection, FooterLink, FooterBottom } from '@arcana-ui/core'",
      "description": "Site footer with link sections, bottom bar, and external link handling.",
      "when": "Add site-wide footer with navigation links and copyright",
      "props": {
        "border": { "type": "boolean", "default": true },
        "variant": { "type": "enum", "values": ["standard", "minimal"], "default": "standard" },
        "FooterSection.title": { "type": "string", "description": "Section heading" },
        "FooterLink.href": { "type": "string" },
        "FooterLink.external": {
          "type": "boolean",
          "default": false,
          "description": "Opens in new tab with rel=noopener"
        }
      },
      "examples": [
        "<Footer><FooterSection title=\"Product\"><FooterLink href=\"/features\">Features</FooterLink></FooterSection><FooterBottom>© 2026 Company</FooterBottom></Footer>"
      ]
    },
    {
      "name": "Hero",
      "import": "import { Hero } from '@arcana-ui/core'",
      "description": "Full-width hero section for landing pages with headline, CTA, and media support",
      "props": {
        "headline": {
          "type": "string",
          "required": true,
          "description": "Main headline (rendered as h1)"
        },
        "subheadline": { "type": "string", "description": "Supporting text" },
        "primaryCTA": {
          "type": "{ label: string; href?: string; onClick?: () => void }",
          "description": "Primary call-to-action"
        },
        "secondaryCTA": {
          "type": "{ label: string; href?: string; onClick?: () => void }",
          "description": "Secondary call-to-action"
        },
        "media": { "type": "ReactNode", "description": "Image, video, or illustration" },
        "variant": { "type": "'centered' | 'split' | 'fullscreen'", "default": "centered" },
        "height": { "type": "'viewport' | 'large' | 'auto'", "default": "auto" },
        "overlay": {
          "type": "boolean",
          "default": false,
          "description": "Dark overlay for text readability"
        },
        "badge": { "type": "string", "description": "Announcement pill above headline" }
      },
      "examples": [
        "<Hero headline=\"Build faster\" subheadline=\"Ship products your customers love.\" primaryCTA={{ label: 'Get Started', href: '/signup' }} badge=\"Now in beta\" />"
      ]
    },
    {
      "name": "FeatureSection",
      "import": "import { FeatureSection } from '@arcana-ui/core'",
      "description": "Grid of feature highlights with icons, titles, and descriptions",
      "props": {
        "title": { "type": "string", "description": "Section heading" },
        "subtitle": { "type": "string", "description": "Section description" },
        "features": {
          "type": "FeatureItemData[]",
          "required": true,
          "description": "Array of { icon?, title, description, link? }"
        },
        "columns": { "type": "2 | 3 | 4", "default": 3 },
        "variant": { "type": "'grid' | 'list' | 'alternating'", "default": "grid" }
      },
      "examples": [
        "<FeatureSection title=\"Features\" features={[{ title: 'Fast', description: 'Built for speed.' }]} />"
      ]
    },
    {
      "name": "Testimonial",
      "import": "import { Testimonial } from '@arcana-ui/core'",
      "description": "Customer testimonial card with quote, author, and optional star rating",
      "props": {
        "quote": { "type": "string", "required": true },
        "author": { "type": "string", "required": true },
        "jobTitle": { "type": "string" },
        "company": { "type": "string" },
        "avatar": { "type": "string", "description": "Avatar image URL" },
        "variant": { "type": "'card' | 'inline' | 'featured'", "default": "card" },
        "rating": { "type": "number", "description": "1-5 star rating" }
      },
      "examples": [
        "<Testimonial quote=\"Amazing product!\" author=\"Jane Doe\" role=\"CEO\" company=\"Acme\" rating={5} />"
      ]
    },
    {
      "name": "PricingCard",
      "import": "import { PricingCard } from '@arcana-ui/core'",
      "description": "Pricing tier card with plan name, price, features list, and CTA",
      "props": {
        "name": { "type": "string", "required": true, "description": "Plan name" },
        "price": { "type": "string | number", "required": true },
        "period": { "type": "string", "description": "e.g. '/month'" },
        "description": { "type": "string" },
        "features": { "type": "PricingFeature[]", "description": "Array of { label, included }" },
        "cta": { "type": "{ label: string; href?: string; onClick?: () => void }" },
        "popular": {
          "type": "boolean",
          "default": false,
          "description": "Highlights as recommended tier"
        },
        "variant": { "type": "'default' | 'compact'", "default": "default" }
      },
      "examples": [
        "<PricingCard name=\"Pro\" price={29} period=\"/month\" features={[{ label: 'Unlimited', included: true }]} cta={{ label: 'Subscribe' }} popular />"
      ]
    },
    {
      "name": "CTA",
      "import": "import { CTA } from '@arcana-ui/core'",
      "description": "Call-to-action section with headline, description, and action buttons",
      "props": {
        "headline": { "type": "string", "required": true },
        "description": { "type": "string" },
        "primaryCTA": { "type": "{ label: string; href?: string; onClick?: () => void }" },
        "secondaryCTA": { "type": "{ label: string; href?: string; onClick?: () => void }" },
        "variant": { "type": "'banner' | 'card' | 'minimal'", "default": "banner" }
      },
      "examples": [
        "<CTA headline=\"Ready to start?\" primaryCTA={{ label: 'Sign Up', href: '/signup' }} variant=\"banner\" />"
      ]
    },
    {
      "name": "StatsBar",
      "import": "import { StatsBar } from '@arcana-ui/core'",
      "description": "Row of key metrics with animated number counting and trend indicators",
      "props": {
        "stats": {
          "type": "StatItemData[]",
          "required": true,
          "description": "Array of { value, label, prefix?, suffix?, trend? }"
        },
        "variant": { "type": "'inline' | 'card'", "default": "inline" }
      },
      "examples": [
        "<StatsBar stats={[{ value: 10000, label: 'Users', suffix: '+', trend: 'up' }]} />"
      ]
    },
    {
      "name": "Timeline",
      "import": "import { Timeline } from '@arcana-ui/core'",
      "description": "Vertical timeline with status dots, connecting lines, and content",
      "props": {
        "items": {
          "type": "TimelineItemData[]",
          "required": true,
          "description": "Array of { title, description, date?, icon?, status? }"
        },
        "variant": { "type": "'standard' | 'compact' | 'alternating'", "default": "standard" }
      },
      "examples": [
        "<Timeline items={[{ title: 'Launch', description: 'Product shipped.', status: 'complete' }]} />"
      ]
    },
    {
      "name": "LogoCloud",
      "import": "import { LogoCloud } from '@arcana-ui/core'",
      "description": "Grid or scrolling strip of partner/client logos with grayscale-to-color hover effect",
      "props": {
        "logos": {
          "type": "LogoItem[]",
          "required": true,
          "description": "Array of { src, alt, href? }"
        },
        "title": { "type": "string", "description": "Section title (e.g. 'Trusted by')" },
        "variant": { "type": "'grid' | 'marquee' | 'fade'", "default": "grid" }
      },
      "examples": [
        "<LogoCloud title=\"Trusted by\" logos={[{ src: '/logos/acme.svg', alt: 'Acme' }]} />"
      ]
    },
    {
      "name": "DataTable",
      "import": "import { DataTable } from '@arcana-ui/core'",
      "description": "Full-featured data table with sorting, filtering, row selection, pagination, sticky header, and loading states. Generic — works with any data shape.",
      "whenToUse": "Dashboards, admin panels, data-heavy views, user management tables",
      "props": {
        "data": { "type": "T[]", "required": true, "description": "Array of data objects" },
        "columns": {
          "type": "ColumnDef<T>[]",
          "required": true,
          "description": "Column definitions with key, header, render, align, width, sticky"
        },
        "sortable": {
          "type": "boolean",
          "default": "false",
          "description": "Enable column header click to sort"
        },
        "filterable": {
          "type": "boolean",
          "default": "false",
          "description": "Show filter search input"
        },
        "selectable": {
          "type": "boolean",
          "default": "false",
          "description": "Show row checkboxes"
        },
        "pagination": {
          "type": "boolean | { pageSize, pageSizeOptions }",
          "default": "false",
          "description": "Enable pagination"
        },
        "striped": {
          "type": "boolean",
          "default": "false",
          "description": "Alternating row colors"
        },
        "hoverable": { "type": "boolean", "default": "true", "description": "Row hover highlight" },
        "stickyHeader": {
          "type": "boolean",
          "default": "false",
          "description": "Sticky header on scroll"
        },
        "loading": { "type": "boolean", "default": "false", "description": "Show skeleton rows" },
        "emptyState": { "type": "ReactNode", "description": "Content when data is empty" }
      },
      "examples": [
        "<DataTable data={users} columns={[{ key: 'name', header: 'Name' }, { key: 'email', header: 'Email' }]} sortable filterable pagination={{ pageSize: 10 }} />"
      ],
      "accessibility": "ARIA sort attributes on sortable headers. Checkbox labels for row selection. Filter input has aria-label."
    },
    {
      "name": "StatCard",
      "import": "import { StatCard } from '@arcana-ui/core'",
      "description": "Compact card displaying a single key metric with optional trend indicator, icon, and comparison text. Uses dl/dt/dd semantics.",
      "whenToUse": "Dashboard KPI grids, metric summaries, analytics overviews",
      "props": {
        "value": {
          "type": "string | number",
          "required": true,
          "description": "Main metric value"
        },
        "label": {
          "type": "string",
          "required": true,
          "description": "What the metric represents"
        },
        "prefix": { "type": "string", "description": "Prefix before value (e.g., '$')" },
        "suffix": { "type": "string", "description": "Suffix after value (e.g., '%')" },
        "trend": {
          "type": "{ value: number, direction: 'up' | 'down' | 'neutral' }",
          "description": "Trend indicator"
        },
        "comparison": {
          "type": "string",
          "description": "Comparison text (e.g., 'vs last month')"
        },
        "icon": { "type": "ReactNode", "description": "Metric icon" },
        "variant": { "type": "'default' | 'compact'", "default": "default" },
        "loading": { "type": "boolean", "default": "false" }
      },
      "examples": [
        "<StatCard value=\"$142,580\" label=\"Revenue\" trend={{ value: 12.3, direction: 'up' }} comparison=\"vs last month\" />"
      ],
      "accessibility": "Uses dl/dt/dd for label/value semantics. Trend has aria-label."
    },
    {
      "name": "ProgressBar",
      "import": "import { ProgressBar } from '@arcana-ui/core'",
      "description": "Horizontal bar showing completion or progress with multiple visual variants and color options.",
      "whenToUse": "File uploads, task completion, loading progress, quota usage",
      "props": {
        "value": { "type": "number", "required": true, "description": "Current progress (0-100)" },
        "max": { "type": "number", "default": "100" },
        "variant": { "type": "'default' | 'striped' | 'animated'", "default": "default" },
        "size": { "type": "'sm' | 'md' | 'lg'", "default": "md" },
        "color": {
          "type": "'primary' | 'success' | 'warning' | 'error' | 'info'",
          "default": "primary"
        },
        "label": { "type": "string", "description": "Accessible label" },
        "showValue": {
          "type": "boolean",
          "default": "false",
          "description": "Show percentage text"
        },
        "indeterminate": {
          "type": "boolean",
          "default": "false",
          "description": "Unknown progress animation"
        }
      },
      "examples": [
        "<ProgressBar value={67} label=\"Upload progress\" showValue />",
        "<ProgressBar value={0} indeterminate label=\"Loading...\" />"
      ],
      "accessibility": "role=progressbar with aria-valuenow, aria-valuemin, aria-valuemax, aria-label. Respects prefers-reduced-motion."
    },
    {
      "name": "KPICard",
      "import": "import { KPICard } from '@arcana-ui/core'",
      "description": "Enhanced stat card with inline SVG sparkline trend visualization. Premium data display component for dashboards.",
      "whenToUse": "Dashboard analytics, KPI tracking, executive summaries with trend data",
      "props": {
        "value": {
          "type": "string | number",
          "required": true,
          "description": "Main metric value"
        },
        "label": { "type": "string", "required": true, "description": "Metric label" },
        "prefix": { "type": "string", "description": "Value prefix" },
        "suffix": { "type": "string", "description": "Value suffix" },
        "trend": {
          "type": "{ value: number, direction: 'up' | 'down' | 'neutral' }",
          "description": "Trend indicator"
        },
        "data": {
          "type": "number[]",
          "description": "Array of values for sparkline (min 2 points)"
        },
        "sparklineColor": { "type": "string", "description": "Custom sparkline color" },
        "target": {
          "type": "{ value: number, label: string }",
          "description": "Target line on sparkline"
        },
        "period": { "type": "string", "description": "Time range label (e.g., 'Last 7 days')" },
        "variant": { "type": "'default' | 'compact'", "default": "default" },
        "loading": { "type": "boolean", "default": "false" }
      },
      "examples": [
        "<KPICard value=\"$142,580\" label=\"Revenue\" trend={{ value: 12.3, direction: 'up' }} data={[95, 102, 98, 115, 120, 130, 142]} period=\"Last 30 days\" />"
      ],
      "accessibility": "dl/dt/dd semantics. Sparkline is aria-hidden (decorative). Trend conveyed via text."
    }
  ],
  "design": {
    "theme": "Warm stone neutrals, deep indigo primary, amber accent",
    "colors": {
      "primary": "#4f46e5",
      "primaryHover": "#4338ca",
      "background": "#fafaf9",
      "text": "#1c1917"
    },
    "typography": {
      "body": "Inter, system-ui, sans-serif",
      "mono": "JetBrains Mono, monospace"
    },
    "radius": "8px",
    "transitions": "150ms ease"
  }
}
